@model List<ProyectoBase.Models.TruckTypeModel>


    <table class="table table-striped mb-0">
        <thead>
            <tr>
                <th> Tipo de Camión </th>
                <th> Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-bold-500"> @Html.DisplayFor(modelItem => item.Description)</td>
                    <td>
                        <input type="button" class="btn btn-primary btn-sm rounded" value="Editar" onclick="callEditTruckTypeModal(@item.Id, '@item.Description', '@item.Preffix')"> |
                        <input type="button" class="btn btn-primary btn-sm rounded" value="Eliminar" onclick="callDeleteTruckTypeModal(@item.Id)" />

                    </td>
                </tr>
            }
        </tbody>
    </table>

