@model List<ProyectoBase.Models.ProductionActionsModel>


    <table class="table table-striped mb-0">
        <thead>
            <tr>
                <th> Acciones de Producción </th>
                <th> Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-bold-500"> @Html.DisplayFor(modelItem => item.Description)</td>
                    <td>
                        <input type="button" class="btn btn-primary btn-sm rounded" value="Editar" onclick="callEditProductionActionsModal(@item.Id, '@item.Description')"> |
                        <input type="button" class="btn btn-primary btn-sm rounded" value="Eliminar" onclick="callDeleteProductionActionsModal(@item.Id)" />

                    </td>
                </tr>
            }
        </tbody>
    </table>

